{
  "name": "kroniki-krolestw",
  "private": true,
  "description": "Kroniki Królestw — turowa strategia fantasy; źródła w src/, gra składana w jeden plik HTML",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "test": "node build.js --check && node --test tests/*.test.js"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
